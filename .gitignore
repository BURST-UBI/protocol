/target
# Runtime LMDB data directories — must never be committed (a committed
# burst_data shipped baked-in live peers that leaked across networks).
/burst_data/
*.mdb
**/*.rs.bk
Cargo.lock
*.swp
*.swo
*~
.env
.idea/
.vscode/
*.log
/references/
